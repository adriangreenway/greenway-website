---
interface Props {
  darkHero?: boolean;
}

const { darkHero = false } = Astro.props;

import { pages } from '../config/site';
import Logo from './Logo.astro';
import LogoInline from './LogoInline.astro';

const publishedPages = pages.filter(p => p.published && p.title !== 'Home');
---

<header class={`header${darkHero ? ' header--dark-hero' : ''}`}>
  <a href="/" class="header__logo header__logo--light">
    <Logo variant="light" />
  </a>
  <a href="/" class="header__logo header__logo--dark">
    <Logo variant="dark" />
  </a>
  <a href="/" class="header__logo-mobile header__logo-mobile--light">
    <LogoInline variant="light" />
  </a>
  <a href="/" class="header__logo-mobile header__logo-mobile--dark">
    <LogoInline variant="dark" />
  </a>
  <nav class="header__nav">
    {publishedPages.map(page => (
      <a href={page.slug} data-cta={page.title === 'Book' ? '' : undefined}>{page.title}</a>
    ))}
  </nav>
  <button class="hamburger" aria-label="Open menu" aria-expanded="false">
    <span></span>
    <span></span>
    <span></span>
  </button>
</header>

<div class="mobile-menu">
  <button class="mobile-menu-close" aria-label="Close menu"></button>
  {publishedPages.map(page => (
    <a href={page.slug}>{page.title}</a>
  ))}
</div>

<script>
  const header = document.querySelector('.header');
  const isDarkHero = header?.classList.contains('header--dark-hero');
  if (isDarkHero) {
    window.addEventListener('scroll', () => {
      const scrolled = window.scrollY > window.innerHeight * 0.8;
      header.classList.toggle('header--scrolled', scrolled);
    }, { passive: true });
  }

  const hamburger = document.querySelector('.hamburger');
  const closeBtn = document.querySelector('.mobile-menu-close');
  const mobileMenu = document.querySelector('.mobile-menu');
  const toggleMenu = (open: boolean) => {
    mobileMenu?.classList.toggle('mobile-menu--open', open);
    document.body.style.overflow = open ? 'hidden' : '';
    hamburger?.setAttribute('aria-expanded', String(open));
  };
  hamburger?.addEventListener('click', () => toggleMenu(true));
  closeBtn?.addEventListener('click', () => toggleMenu(false));
  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => toggleMenu(false));
  });
</script>
